server {
    listen 3000;
    server_name _;

    location = /health {
        add_header Content-Type text/plain;
        return 200 'OK';
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}
